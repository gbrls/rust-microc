int y;

int max(int a, int b)
{
    int ans;
    if (a < b)
    {
        ans = b;
    }
    else
    {
        ans = a;
    }

    ans;
}

int mmax(int a, int b, int c)
{
    int x;
    x = max(a, b);
    max(x, c);
}

int _start()
{
    int z;
    z = 10;
    if (z < 10)
    {
        y = max(10, 3);
    }
    else
    {
        y = max(2, 13);
    }

    y = y + z;

    int k;
    k = mmax(1, 2, 3);
}
y;